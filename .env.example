# Copy to .env and fill in. .env is gitignored.

# Required: OpenRouter API key used for message classification.
OPENROUTER_API_KEY=

# Optional: Pushover credentials. Only needed if notifications.pushover is true in config.json.
PUSHOVER_APP_TOKEN=
PUSHOVER_USER_KEY=
